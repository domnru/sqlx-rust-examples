version: '3.9'

services:
  postgres:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=unicorn_postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=sql_lernen